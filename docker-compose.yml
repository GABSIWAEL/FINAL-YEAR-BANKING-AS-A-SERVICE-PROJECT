version: "3.9"

services:

  account-db:
    image: postgres:15-alpine
    container_name: account-db
    environment:
      POSTGRES_DB: accountdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5432:5432"
    volumes:
      - account-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  account.dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:latest
    container_name: dashboard_ACCOUNT
    ports:
      - "18888:18888"
      - "18889:18889"

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.13.0
    container_name: elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.monitoring.collection.enabled=true
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    ports:
      - "9200:9200"
    volumes:
      - esdata:/usr/share/elasticsearch/data

  kibana:
    image: docker.elastic.co/kibana/kibana:8.13.0
    container_name: kibana
    environment:
      ELASTICSEARCH_HOSTS: "http://elasticsearch:9200"
      xpack.security.enabled: "false"
      xpack.monitoring.enabled: "true"
      xpack.monitoring.ui.container.elasticsearch.enabled: "true"
      xpack.fleet.enabled: "false"
      xpack.fleet.agents.enabled: "false"
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch

  account-service:
    build:
      context: .
      dockerfile: OpenBanking_ACCOUNT_V1/Dockerfile
    container_name: account-service
    ports:
      - "8088:8088"
      - "8089:8089"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=account-db;Port=5432;Username=postgres;Password=mysecretpassword;Database=accountdb
    depends_on:
      account-db:
        condition: service_healthy

  branch-db:
    image: mysql:5.7
    container_name: branch-db
    environment:
      MYSQL_ROOT_PASSWORD: mysecretpassword
      MYSQL_DATABASE: branchdb
    ports:
      - "3306:3306"
    volumes:
      - branch-db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  branch-service:
    build:
      context: .
      dockerfile: OpenBanking_BRANCH_V1/Dockerfile
    container_name: branch-service
    ports:
      - "8084:8084"
      - "8085:8085"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=branch-db;Port=3306;Database=branchdb;User=root;Password=mysecretpassword;
    depends_on:
      branch-db:
        condition: service_healthy

  card-db:
    image: postgres:13
    container_name: card-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: carddb
    ports:
      - "5433:5432"
    volumes:
      - card-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  card-service:
    build:
      context: .
      dockerfile: OpenBanking_CARD_V1/Dockerfile
    container_name: card-service
    ports:
      - "8086:8086"
      - "8087:8087"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=card-db;Port=5432;Username=postgres;Password=mysecretpassword;Database=carddb
    depends_on:
      card-db:
        condition: service_healthy


  atm-service:
    build:
      context: .
      dockerfile: OpenBanking_ATM_V1/Dockerfile
    container_name: atm-service
    ports:
      - "8082:8082"
      - "8083:8083"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - mongodb://atm-db:27017/atmdb
    depends_on:
      atm-db:
        condition: service_healthy

  atm-db:
    image: mongo:latest
    container_name: atm-db
    ports:
      - "27017:27017"
    healthcheck:
      test: ["CMD-SHELL", "mongosh --eval 'db.adminCommand(\"ping\")' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10


  mysqlAuth-db:
    image: mysql:5.7
    container_name: authdb
    environment:
      MYSQL_ROOT_PASSWORD: mysecretpassword
      MYSQL_DATABASE: authdb
    ports:
      - "3307:3306"
    volumes:
      - authdb-data:/var/lib/mysql
      - ./init-auth-user.sql:/docker-entrypoint-initdb.d/init-auth-user.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  authenticator-service:
    build:
      context: .
      dockerfile: OpenBanking_AUTHENTICATOR_V1/Dockerfile
    container_name: authenticator-service
    ports:
      - "8090:8090"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=server=authdb;port=3306;database=authdb;user=appuser;password=apppassword;SslMode=none;
    depends_on:
      mysqlAuth-db:
        condition: service_healthy
    user: root
    volumes:
      - data-protection-keys:/root/.aspnet/DataProtection-Keys

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus

  redis:
    image: redis:latest
    container_name: redisaouth
    ports:
      - "6379:6379"

  frontend:
    build:
      context: ./OpenBanking_ANGULAR_V1
      dockerfile: Dockerfile
    ports:
      - "8083:80"
    volumes:
      - ./uploads:/shared/openBanking/uploads
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - default  


  notification-service:
    build:
      context: .
      dockerfile: OpenBanking_NOTIFICATION_V1/Dockerfile
    container_name: notification-service
    ports:
      - "9094:9094"
      - "9095:9095"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - RabbitMQ__HostName=rabbitmq
      - RabbitMQ__UserName=kalo
      - RabbitMQ__Password=kalo
    depends_on:
      rabbitmq:
        condition: service_healthy

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    restart: always
    ports:
      - "5672:5672"     # Messaging
      - "15672:15672"   # Management UI
    environment:
      RABBITMQ_DEFAULT_USER: kalo
      RABBITMQ_DEFAULT_PASS: kalo
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - rabbitmq-lib:/var/lib/rabbitmq/
      - rabbitmq-log:/var/log/rabbitmq

networks:
  default:
    driver: bridge

volumes:
  account-db-data:
  branch-db-data:
  card-db-data:
  authdb-data:
  rabbitmq-lib:
  rabbitmq-log:
  esdata:
  grafana-data:
  data-protection-keys:
